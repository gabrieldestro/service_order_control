<Window x:Class="ServiceOrder.Views.Dialog.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        SizeToContent="WidthAndHeight"
        Background="Transparent"
        AllowsTransparency="True"
        ShowInTaskbar="False">
    <Border Background="White" Padding="20" CornerRadius="10" BorderBrush="#D3D3D3" BorderThickness="1">
        <StackPanel>
            <TextBlock x:Name="TitleText" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock x:Name="MessageText" FontSize="16" Margin="0,0,0,20" TextWrapping="Wrap"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Sim" Style="{StaticResource StandardButtonStyle}" Width="80" Margin="0,0,10,0" Click="Yes_Click"/>
                <Button Content="Não" Style="{StaticResource StandardButtonStyle}" Width="80" Click="No_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
