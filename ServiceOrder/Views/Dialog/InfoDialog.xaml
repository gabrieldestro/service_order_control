<Window x:Class="ServiceOrder.Views.Dialog.InfoDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        SizeToContent="WidthAndHeight"
        Background="Transparent"
        AllowsTransparency="True"
        ShowInTaskbar="False">
    <Border Background="White" Padding="20" CornerRadius="10" BorderBrush="#D3D3D3" BorderThickness="1">
        <StackPanel>
            <TextBlock x:Name="TitleText" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock x:Name="MessageText" FontSize="16" Margin="0,0,0,20" TextWrapping="Wrap"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="OK" Style="{StaticResource StandardButtonStyle}" Width="80" Click="Ok_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
