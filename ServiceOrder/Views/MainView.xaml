<Window x:Class="ServiceOrder.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Projetos" Height="600" Width="1000">
    <DockPanel>
        
        <!-- Barra de navegação -->
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Background="White" Height="50" VerticalAlignment="Center">
            <Button Content="Projetos" FontSize="16" Click="ShowOrders" Style="{StaticResource FlatNavButtonStyle}"/>
            <Button Content="Prazos" FontSize="16" Click="ShowDeadlines" Style="{StaticResource FlatNavButtonStyle}"/>
            <Button Content="Clientes" FontSize="16" Click="ShowClients" Style="{StaticResource FlatNavButtonStyle}"/>
            <Button Content="Companhias" FontSize="16" Click="ShowElectricCompanies" Style="{StaticResource FlatNavButtonStyle}"/>
            <Button Content="Opções" FontSize="16" Click="ShowOptions" Style="{StaticResource FlatNavButtonStyle}"/>
        </StackPanel>

        <!-- Conteúdo central -->
        <ContentControl x:Name="MainContent" />
    </DockPanel>
</Window>