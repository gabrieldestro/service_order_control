<Window x:Class="ServiceOrder.ElectricCompanyDetailView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ServiceOrder"
        mc:Ignorable="d"
        Title="Cadastro de Companhia Elétrica" Height="450" Width="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Nome da Companhia:" Margin="0,0,0,5"/>
            <TextBox x:Name="CompanyNameTextBox" Height="30"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0">
            <Button Content="Salvar" Width="100" Margin="5" Click="OnSaveClick"/>
            <Button Content="Cancelar" Width="100" Margin="5" Click="OnCancelClick"/>
        </StackPanel>
    </Grid>
</Window>
