<UserControl x:Class="ServiceOrder.ElectricCompanyListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ServiceOrder"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Filtros -->
        <Border Grid.Row="0" Margin="0,0,0,10" Padding="10" Background="White" CornerRadius="5" BorderBrush="#D3D3D3" BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <StackPanel Orientation="Vertical" Margin="5">
                    <Label Content="Nome:" FontWeight="Bold"/>
                    <TextBox x:Name="SearchNameTextBox" Width="200" Height="25" Margin="5"/>
                </StackPanel>
                <StackPanel Orientation="Vertical" Margin="5">
                    <Label Content="CNPJ:" FontWeight="Bold"/>
                    <TextBox x:Name="SearchCnpjTextBox" Width="150" Height="25" Margin="5"/>
                </StackPanel>
                <Button Content="Filtrar" Width="100" Height="30" Margin="10" Click="OnFilterClick"/>
                <Button Content="Limpar" Width="100" Height="30" Margin="10" Click="OnClearFiltersClick"/>
            </StackPanel>
        </Border>

        <!-- Grade de Dados -->
        <DataGrid x:Name="ElectricCompanyDataGrid" Grid.Row="1" Margin="0,0,0,10" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Ações" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Editar" Width="60" Margin="5" Click="OnEditClick"/>
                                <Button Content="Excluir" Width="60" Margin="5" Click="OnDeleteClick"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="CNPJ" Binding="{Binding Cnpj}" Width="150"/>
                <DataGridTextColumn Header="Criado em" Binding="{Binding CreatedDate, StringFormat=dd/MM/yyyy}" Width="120"/>
                <DataGridTextColumn Header="Atualizado em" Binding="{Binding LastUpdated, StringFormat=dd/MM/yyyy}" Width="120"/>
                
            </DataGrid.Columns>
        </DataGrid>

        <!-- Contagem de Registros -->
        <Label x:Name="CompanyRecordCountLabel" Grid.Row="2" HorizontalAlignment="Right" Margin="0,5,0,5" FontSize="12" Foreground="#555"/>
        <!-- Botões -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0">
            <Button Content="Cadastro Companhia" Width="140" Height="40" Margin="10,0,0,0"
                Background="white" BorderThickness="1" Click="OnNewCompanyClick"/>
        </StackPanel>
    </Grid>
</UserControl>
