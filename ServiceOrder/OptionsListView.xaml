<UserControl x:Class="ServiceOrder.OptionsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ServiceOrder"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="0">
            <Button Content="Backup" x:Name="BackupButton" Width="120" Height="40" Margin="10,0,0,0"
            Background="white" BorderThickness="1" Click="OnBackupClick"/>
        </StackPanel>
    </Grid>
</UserControl>
